#include "Pods-Buoy.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Buoy" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Buoy"
OTHER_LDFLAGS = -ObjC ${PODS_BUOY_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}